#include <stdio.h>

int main()
{
	printf("the "); // Use printf for output
	int n;
	scanf("%d", &n);   // Use scanf for input
	printf("%d\n", n); // Use printf to print the value of n and add a newline character
	//fflush(stdout);	   // Flush the output buffer
	return 0;		   // Return statement should have a semicolon at the end
}
